// External Modules
import React from "react";
import ReactDOM from "react-dom";

// Internal Modules
import { App } from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
